interface:
  display_name: "UMB"
  short_description: "Use the live UMB Chrome bridge for browser-only work."
  default_prompt: "Use $use-my-browser for browser-only work through the live UMB bridge and the user's real Chrome session. Do not substitute HTTP, search, or another browser surface; report when the bridge is unavailable."
