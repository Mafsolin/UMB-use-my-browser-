{
  "name": "umb-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "UMB (Use My Browser) workspace",
  "packageManager": "pnpm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "@types/node": "^24.0.14",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "ws": "^8.21.0"
  }
}
